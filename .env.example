# Configurações de conexão para o banco de dados PostgreSQL.
# Copie este arquivo para .env e preencha com suas credenciais reais.

# Endereço do servidor do banco de dados (ex: localhost ou um IP)
DB_HOST=localhost

# Porta em que o PostgreSQL está rodando (padrão é 5432)
DB_PORT=5432

# Nome do banco de dados a ser utilizado
DB_NAME=emissions_db

# Nome de usuário para acessar o banco de dados
DB_USER=postgres_user

# Senha para o usuário do banco de dados
DB_PASSWORD=strong_password